# NOTE:
# - numpy is installed via conda (see environment.yml); do NOT pip-install numpy here.
# - openai-whisper is installed by setup_env.sh (default WHISPER_VERSION=20230314). See below.

setuptools==68.2.2

# Core
moviepy==1.0.3
requests==2.32.3
yt-dlp==2024.8.6

# Imaging / MoviePy stack
Pillow==9.5.0
imageio==2.34.0
imageio-ffmpeg==0.4.9

# MoviePy dependencies
decorator==4.4.2
tqdm==4.66.1
proglog==0.1.10

# Optional helpers
python-dotenv==1.0.1

# Whisper (setup_env.sh installs this; keeping it here documents the exact version)
#openai-whisper==20230314
